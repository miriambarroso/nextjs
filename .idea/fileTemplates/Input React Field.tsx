type Props = {
register; 
error; 
required?: boolean
label?: string;
name?: string;
};

const ${NAME} = ({register, error, required=false, label='', name=''}: Props) => {
  return <></>;
};

export default ${NAME};
